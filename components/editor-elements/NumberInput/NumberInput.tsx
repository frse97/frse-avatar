import React from "react";
import { INumberInputProps } from "../../../model/editor.model";

const NumberInput: React.FC<INumberInputProps> = props => {
    const { } = props;
    return (
        <input type="number"></input>
    );
};

export default NumberInput;
